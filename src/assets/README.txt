Drop your logo here as:   rane-logo.png

The header picks it up automatically (any of .png .svg .jpg .jpeg .webp works).
Until the file exists, the header shows a plain "Rane" wordmark instead of
breaking the build.
